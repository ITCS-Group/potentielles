<svg width="120" height="80" viewBox="0 0 120 80" xmlns="http://www.w3.org/2000/svg">
  <circle cx="60" cy="40" r="30" fill="#003399"/>
  <g fill="#FFCC00">
    {Array.from({ length: 12 }).map((_, i) => (
      <path
        key={i}
        d="M60 40l15-5"
        transform={`rotate(${i * 30} 60 40)`}
      />
    ))}
  </g>
</svg>
